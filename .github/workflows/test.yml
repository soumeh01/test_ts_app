name: test-schedule

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    # Test schedule - triggers at specific times to verify scheduling works
    - cron: "23 13 13 2 *"  # Feb 13, 2026 at 13:23 UTC
    - cron: "25 13 13 2 *"  # Feb 13, 2026 at 13:25 UTC
    - cron: "27 13 13 2 *"  # Feb 13, 2026 at 13:27 UTC
    - cron: "30 13 13 2 *"  # Feb 13, 2026 at 13:30 UTC
  workflow_dispatch:

jobs:
  build_artifacts:
    name: Build Artifacts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      
      - name: Verify schedule trigger
        run: |
          echo "Workflow triggered at $(date)"
          echo "Event: ${{ github.event_name }}"
